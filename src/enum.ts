export enum REMOTE {
  INIT = "Init...",
  NOT_SET = "Remote not set.",
  NOT_EXISTING_DIR = "Remote directory not existing.",
  OK = "Remote directory set.",
}

export enum LOCAL {
  INIT = "Init...",
  NOT_SET = "Local not set.",
  NOT_EXISTING_DIR = "Local directory not existing.",
  OK = "Local directory set.",
}
