{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "#/definitions/BackupOptions",
  "definitions": {
    "BackupOptions": {
      "type": "object",
      "properties": {
        "$schema": {
          "type": "string",
          "description": "The JSON Schema used to check the syntax of this config file"
        },
        "sh": {
          "type": "string",
          "description": "The path of the sh.exe on your installation",
          "default": "C:\\Program Files\\Git\\bin\\sh.exe"
        },
        "local": {
          "type": "string",
          "description": "Directory containing all git local repositories."
        },
        "intervalInSecond": {
          "type": "number",
          "description": "Number of second between two backups"
        },
        "port": {
          "type": "number",
          "description": "Port that the admin server listens"
        }
      },
      "required": [],
      "additionalProperties": false
    }
  }
}
